# Defaults for templates — human should replace or remove

# Generated/deps
node_modules/
.venv/
__pycache__/

# Human-only reference — excluded from default @-mentions / indexing (see README).
_human-docs/
